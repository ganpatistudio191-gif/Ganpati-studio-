"use client";
import { FormEvent, useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";
export default function WatermarkAdmin(){const [logo,setLogo]=useState(""),[position,setPosition]=useState("bottom-right"),[opacity,setOpacity]=useState("0.8"),[size,setSize]=useState("5"),[message,setMessage]=useState("");useEffect(()=>{supabase?.from("site_settings").select("value").eq("key","watermark").maybeSingle().then(({data})=>{const v=data?.value as Record<string,string>|undefined;if(v){setLogo(v.logo||"");setPosition(v.position||"bottom-right");setOpacity(v.opacity||"0.8");setSize(v.size||"5");}})},[]);async function save(e:FormEvent){e.preventDefault();if(!supabase)return;const {error}=await supabase.from("site_settings").upsert({key:"watermark",value:{logo,position,opacity,size},updated_at:new Date().toISOString()});setMessage(error?.message||"Watermark settings saved.");}return <main className="min-h-screen bg-[#080808] px-6 py-10 text-white"><div className="mx-auto max-w-2xl"><a href="/admin" className="text-sm text-[#d8b56a]">← Back to Admin</a><h1 className="mt-5 text-4xl font-semibold">Watermark Settings</h1><p className="mt-3 text-white/45">Set the logo and placement used on downloadable copies.</p><form onSubmit={save} className="mt-8 space-y-5 rounded-3xl border border-white/10 bg-[#0d0d0d] p-7"><label className="block text-sm">Logo image URL<input required value={logo} onChange={e=>setLogo(e.target.value)} placeholder="Upload logo, then paste Supabase URL" className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none"/></label><label className="block text-sm">Position<select value={position} onChange={e=>setPosition(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3"><option value="top-left">Top left</option><option value="top-right">Top right</option><option value="bottom-left">Bottom left</option><option value="bottom-right">Bottom right</option><option value="center">Center</option></select></label><label className="block text-sm">Opacity (0–1)<input type="number" min="0" max="1" step="0.05" value={opacity} onChange={e=>setOpacity(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none"/></label><label className="block text-sm">Size (% of image width)<input type="number" min="1" max="25" value={size} onChange={e=>setSize(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none"/></label><button className="rounded-full bg-[#d8b56a] px-6 py-3.5 font-semibold text-black">Save watermark settings</button>{message&&<p className="text-sm text-[#d8b56a]">{message}</p>}</form></div></main>}
